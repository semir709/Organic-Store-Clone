import React from "react";

const SectionMessage = ({ title }) => {
  return (
    <div className="pt-[100px] bg-global-color-4">
      <h1 className="text-5xl font-semibold text-center">{title}</h1>
    </div>
  );
};

export default SectionMessage;
